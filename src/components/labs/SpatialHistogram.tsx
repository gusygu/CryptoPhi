"use client";

type SpatialHistogramProps = {
  // TODO: define: points, bins, width/height, projection, etc.
};

export function SpatialHistogram(_props: SpatialHistogramProps) {
  return (
    <div className="rounded-2xl border p-4">
      <div className="text-sm opacity-70">SpatialHistogram (WIP)</div>
    </div>
  );
}
